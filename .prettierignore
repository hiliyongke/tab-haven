# 锁文件由包管理器生成，格式化它会污染 diff 且与 `pnpm install --frozen-lockfile`
# 的校验无益（此前 `format:check` 一直把它判为不合规却从没人提交格式化结果）。
pnpm-lock.yaml

# 构建产物与覆盖率报告（测试自动生成，不进版本库）
.output/
.wxt/
coverage/

# 审计/评审报告（由工具生成的长文归档，非源码）。
# CI 对全仓跑 `format:check`，把生成的报告纳入格式化检查会让报告写作与构建互相牵制。
deliverables/
