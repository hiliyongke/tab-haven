# 隐私说明（Privacy）

> **TabHaven（页港）是一个本地优先、无账号、无云端的扩展。你的数据只存在于你自己的浏览器里，永远不会离开你的设备。**

本说明依据产品需求文档（PRD）的隐私红线（FR-D10.5）编写，并在每个版本发布前随隐私回归检查同步更新。

---

## 1. 核心承诺

- **无后端**：TabHaven 没有任何服务器，不提供账号体系，不与任何远端服务通信。
- **零上传**：扩展代码不包含任何网络请求库，也不发起任何出站网络请求（无 `fetch` / `XMLHttpRequest` / `WebSocket` 等）。所有功能在本地完成。
- **无遥测**：不收集使用统计、不埋点、不追踪。
- **数据不离设备**：所有用户数据仅存于浏览器本地存储（`chrome.storage.local`）。

---

## 2. 我们申请的权限及其用途

权限清单已在 `manifest.json` 中**冻结为最小集合**，任何新增权限都须经需求评审并同步本说明。

| 权限 | 用途 | 是否触网 |
| --- | --- | --- |
| `storage` | 读写本地存储，保存你的固定文件夹、固定图标、设置等偏好数据；同步镜像走 `chrome.storage.sync`（浏览器账号通道） | 否（仅本地） |
| `tabs` | 读取当前窗口的标签页（标题、网址、图标、静音/固定状态）以在侧边栏中组织与展示；「搜索所有窗口」开启时读取其他窗口标签元数据 | 否 |
| `tabGroups` | 读取与管理 Chrome 原生标签组，支持"保存为固定文件夹" | 否 |
| `sidePanel` | 在浏览器侧边栏中展示面板（主形态） | 否 |
| `commands` | 注册浏览器级快捷键（聚焦搜索 / 打开面板 / 定位激活 / 休眠非激活） | 否 |
| `alarms` | 在用户主动开启自动休眠后按周期检查标签状态 | 否 |
| `contextMenus` | 在网页/链接/标签栏/工具栏图标右键菜单提供休眠、固定、加入文件夹、搜索等快捷操作 | 否 |
| `omnibox` | 地址栏输入 `th` 命令后搜索标签、固定条目与文件夹 | 否 |
| `sessions` | 在撤销历史面板中展示并恢复浏览器最近关闭的标签/窗口 | 否 |
| `bookmarks` | 仅在用户主动操作时：固定文件夹导出为书签 / 从书签栏导入固定文件夹 | 否 |
| `notifications` | 自动休眠执行时在本机弹出通知（可一键打开面板撤销） | 否 |

> 说明：`tabs` 权限会让扩展在 `chrome://extensions` 中显示"读取你的浏览历史"的通用提示——这是 Chrome 对该权限的标准描述。TabHaven **只读取当前窗口的标签页元数据用于本地展示与组织**，不会读取、上传或记录你的浏览历史。

---

## 3. 我们存储的数据

所有数据存于 `chrome.storage.local`，**仅本机可见**。键名均带版本后缀（如 `tabhaven.fixed-folders.v1`）。

| 数据类型 | 内容 | 是否含网址 |
| --- | --- | --- |
| 固定文件夹 | 你创建的文件夹及其条目（标题、网址、图标） | 是（仅你主动添加的） |
| 永久固定图标 | 顶部固定图标的网址与标题 | 是（仅你主动固定的） |
| 设置 | 主题偏好、语言、聚合阈值、休眠白名单等界面配置 | 否（白名单为域名列表） |
| 撤销栈 | 近期关闭操作的记录（用于“撤销”），按设置保存在本地或仅保留当前会话 | 否（仅标签 URL、位置、分组和状态元数据） |
| 自动休眠台账 | 最近一次自动休眠涉及的标签 id 列表（用于“全部唤醒”撤销），休眠批次被撤销或失效后自动清除 | 否 |
| 同步镜像 | 固定文件夹、固定图标与设置的分块镜像（存于 `chrome.storage.sync`，随你的浏览器账号同步） | 是（仅你主动固定的网址） |

我们**不存储**：浏览历史、Cookie、表单数据、密码、账号信息，以及任何你未在扩展中主动创建的内容。

---

## 4. 跨设备同步的说明

TabHaven 自身没有服务器、不自建同步通道。为方便换机迁移，固定文件夹、固定图标与设置会以分块形式镜像到浏览器的同步存储（`chrome.storage.sync`）——这走的是**你自己的浏览器账号通道**，产品不经手、也看不到这些数据。

- **同步范围**：仅固定集合与设置（小数据量）；撤销栈、休眠台账、折叠状态、当前打开标签不参与同步。
- **首次启动**：新设备首次安装 TabHaven 时，若浏览器账号中存在镜像数据且本机没有本地数据，会自动恢复镜像内容；此后以本机数据为准（本地优先，最后修改的设备生效）。
- **配额降级**：若数据量超出浏览器同步配额，镜像静默失效，不影响本地数据。
- **关闭同步**：在浏览器设置中关闭对应账号的扩展数据同步即可停止；TabHaven 不读取、不依赖其他同步数据。

---

## 5. 数据导出与删除

- **导出**：你可在设置页将固定文件夹、永久固定图标、折叠状态和设置导出为 JSON 文件，文件保存在你指定的本地位置，完全由你控制。当前打开的标签和撤销栈不包含在导出文件中。
- **导入**：导入会先在本地校验文件格式，再覆盖上述固定数据；不会自动打开、关闭或修改当前标签。
- **删除**：卸载扩展会清除 `chrome.storage.local` 中属于 TabHaven 的全部数据；你也可以手动在浏览器扩展管理页清除站点数据。

---

## 6. 隐私回归检查

每次发布前，仓库内的隐私回归脚本（`scripts/privacy_check.py`）会校验三项：

1. **权限 diff 为零变更**或已在本说明中记录；
2. **网络请求 diff 为零**（源码中不存在任何出站网络调用）；
3. **存储数据 diff** 的新增项均已文档化。

只有通过上述检查，版本方可发布。

---

## 7. 联系与透明

如发现任何与本文不符的行为，请通过项目仓库提交 issue。我们以"数据不离设备"为不可妥协的产品底线。
