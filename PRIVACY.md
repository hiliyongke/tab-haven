# 隐私说明（Privacy）

> **TabHaven（页港）是一个本地优先、无自有账号、无自有服务器的扩展。你的数据默认只存在于你自己的浏览器里；其中固定集合与设置可通过你自己的浏览器账号同步通道镜像到你的其他设备，TabHaven 不经手、也看不到这些数据。**

本说明依据产品需求文档（PRD）的隐私红线（FR-D10.5）编写，并在每个版本发布前随隐私回归检查同步更新。

---

## 1. 核心承诺

- **无自有服务器**：TabHaven 没有自己的服务器，不提供账号体系，不与任何 TabHaven 侧远端服务通信。
- **零上传**：扩展代码不包含任何网络请求库，也不发起任何出站网络请求（无 `fetch` / `XMLHttpRequest` / `WebSocket` 等）。所有功能在本地完成。
- **无遥测**：不收集使用统计、不埋点、不追踪。
- **本地优先**：全部数据以 `chrome.storage.local` 为主存储；固定文件夹、固定图标与设置会额外镜像到 `chrome.storage.sync`，经**浏览器账号自己的同步通道**在你的设备间同步（见第 4 节）。
- **准确表述**：以上组合应表述为「无自有账号、无自有服务器」。「数据永不离开设备」并不准确——开启浏览器同步时，上述镜像数据会经由浏览器厂商的同步通道传输。

---

## 2. 我们申请的权限及其用途

权限清单已在 `manifest.json` 中**冻结为最小集合**，任何新增权限都须经需求评审并同步本说明。

| 权限 | 用途 | 是否触网 |
| --- | --- | --- |
| `storage` | 读写本地存储，保存你的固定文件夹、固定图标、设置等偏好数据；同步镜像走 `chrome.storage.sync`（浏览器账号通道） | 否（仅本地） |
| `tabs` | 读取当前窗口的标签页（标题、网址、图标、静音/固定状态）以在侧边栏中组织与展示；「搜索所有窗口」开启时读取其他窗口标签元数据 | 否 |
| `tabGroups` | 读取与管理 Chrome 原生标签组，支持"保存为固定文件夹" | 否 |
| `sidePanel` | 在浏览器侧边栏中展示面板（主形态） | 否 |
| `commands` | 注册浏览器级快捷键（聚焦搜索 / 打开面板 / 定位激活 / 休眠非激活） | 否 |
| `alarms` | 在用户主动开启自动休眠后按周期检查标签状态 | 否 |
| `contextMenus` | 在网页/链接/标签栏/工具栏图标右键菜单提供休眠、固定、加入文件夹、搜索等快捷操作 | 否 |
| `omnibox` | 地址栏输入 `th` 命令后搜索标签、固定条目与文件夹 | 否 |
| `sessions` | 在撤销历史面板中展示并恢复浏览器最近关闭的标签/窗口 | 否 |
| `bookmarks` | 仅在用户主动操作时：固定文件夹导出为书签 / 从书签栏导入固定文件夹 | 否 |
| `notifications` | 自动休眠执行时在本机弹出通知（通知内容仅为本机文案，撤销入口在侧边栏的通知面板内） | 否 |
| `declarativeNetRequest` | 开发者「指定站点禁缓存」：按你在设置中添加的站点/URL 前缀，把命中响应的 `Cache-Control` 等头改写为 no-store（默认关闭）。不拦截、不读取请求内容 | 否（只改写浏览器内既有响应的头，不发起任何请求） |
| `scripting` | 保留用于「禁用缓存」警示条能力；当前版本未实际注入任何脚本，权限处于声明未使用状态，后续如彻底移除会同步更新本表 | 否 |
| 全站 host 权限（optional，按需） | 上述两项能力的生效范围。**默认不授予**：仅在你主动开启「禁用缓存」开关时由浏览器弹窗询问；拒绝则功能保持关闭。可在浏览器扩展详情页随时收回 | 否（用于改写响应头与注入警示条，不读取页面内容、不上传任何数据） |

> 说明：`tabs` 权限会让扩展在 `chrome://extensions` 中显示"读取你的浏览历史"的通用提示——这是 Chrome 对该权限的标准描述。TabHaven **只读取当前窗口的标签页元数据用于本地展示与组织**，不会读取、上传或记录你的浏览历史。

---

## 3. 我们存储的数据

数据**主存储**为 `chrome.storage.local`（仅本机可见），固定集合与设置另有 `chrome.storage.sync` 镜像（见第 4 节）。键名均带版本后缀（如 `tabhaven.fixed-folders.v1`）。

| 数据类型 | 内容 | 是否含网址 |
| --- | --- | --- |
| 固定文件夹 | 你创建的文件夹及其条目（标题、网址、图标） | 是（仅你主动添加的） |
| 永久固定图标 | 顶部固定图标的网址与标题 | 是（仅你主动固定的） |
| 设置 | 主题偏好、语言、聚合阈值、休眠白名单、禁缓存站点列表等界面配置 | 否（白名单与禁缓存列表为域名/URL 前缀列表） |
| 撤销栈 | 近期关闭操作的记录（用于“撤销”），按设置保存在本地或仅保留当前会话 | 是（标签 URL、位置、分组和状态元数据；不参与同步） |
| 自动休眠台账 | 最近一次自动休眠涉及的标签 id 列表（用于“全部唤醒”撤销），休眠批次被撤销或失效后自动清除 | 否 |
| 快照与归档 | 命名快照、关窗自动快照、归档与轻量空间快照（标题、网址、固定/静音/分组信息） | 是（快照与归档本质就是网址集合；不参与同步） |
| 同步镜像 | 固定文件夹、固定图标与设置的分块镜像（存于 `chrome.storage.sync`，随你的浏览器账号同步） | 是（仅你主动固定的网址） |

我们**不存储**：浏览历史、Cookie、表单数据、密码、账号信息，以及任何你未在扩展中主动创建的内容。

---

## 4. 跨设备同步的说明

TabHaven 自身没有服务器、不自建同步通道。为方便换机迁移，固定文件夹、固定图标与设置会以分块形式镜像到浏览器的同步存储（`chrome.storage.sync`）——这走的是**你自己的浏览器账号通道**，产品不经手、也看不到这些数据。

- **同步范围**：仅固定集合与设置（小数据量）；撤销栈、休眠台账、折叠状态、当前打开标签不参与同步。
- **首次启动**：新设备首次安装 TabHaven 时，若浏览器账号中存在镜像数据且本机没有本地数据，会自动恢复镜像内容；此后以本机数据为准（本地优先，最后修改的设备生效）。
- **配额降级**：若数据量超出浏览器同步配额，镜像静默失效，不影响本地数据。
- **关闭同步**：在浏览器设置中关闭对应账号的扩展数据同步即可停止；TabHaven 不读取、不依赖其他同步数据。

---

## 5. 数据导出与删除

- **导出**：你可在设置页将固定文件夹、永久固定图标、折叠状态、设置，以及全部快照与归档导出为 JSON 文件（formatVersion 2），文件保存在你指定的本地位置，完全由你控制。当前打开的标签和撤销栈不包含在导出文件中。
- **导入**：导入会先在本地校验文件格式，再写入上述数据；任一分区写入失败会整体回滚并提示失败，不会留下半套数据。导入旧版 v1 备份时，其中不含快照，你现有的快照会被保留（不会被清空）。导入不会自动打开、关闭或修改当前标签。
- **删除**：卸载扩展会清除 `chrome.storage.local` 中属于 TabHaven 的全部数据；你也可以手动在浏览器扩展管理页清除站点数据。

---

## 6. 隐私回归检查

每次发布前，仓库内的隐私回归脚本（`pnpm check:privacy`，入口 `scripts/privacy-check.mjs`，需先 `pnpm build`）会校验三项：

1. **权限 diff 为零变更**或已在本说明中记录；
2. **网络请求 diff 为零**（源码中不存在任何出站网络调用）；
3. **存储数据 diff** 的新增项均已文档化。

只有通过上述检查，版本方可发布。

---

## 7. 联系与透明

如发现任何与本文不符的行为，请通过项目仓库提交 issue。我们以「无自有账号、无自有服务器、零出站网络请求」为不可妥协的产品底线。
