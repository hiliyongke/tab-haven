# 更新日志（Changelog）

本文件记录 TabHaven（页港）公开发布的版本变更。版本号遵循语义化版本（SemVer）。

---

## [1.1.0] — 未发布

> 产品评估驱动的一轮能力补齐（评估见 `docs/PRODUCT-REVIEW-V1.0.md`）。不含多选批量（已决策不做）。

### 新增能力

**快捷入口与无处不在**
- 右键菜单体系：网页右键（休眠此页 / 固定此页 / 按站点搜索 / 加入固定文件夹）、链接右键（加入固定文件夹）、标签栏右键（休眠 / 按站点搜索）、工具栏图标右键（开面板 / 休眠非激活 / 设置）
- 地址栏命令：输入 `th` + 关键词直达标签（跨窗口）、固定图标、文件夹条目，或一键打开整个文件夹
- 工具栏角标：实时显示重复组数（红色警示）或标签总数，悬停可见休眠数摘要

**安全网扩展**
- 撤销历史面板：浏览全部撤销批次并一键恢复任意一步；桥接浏览器「最近关闭」（`chrome.sessions`）一键还原原生关闭的标签/窗口
- 自动休眠三件套：域名白名单（固定条目默认豁免）+ 完成通知 + 可一键「全部唤醒」（台账撤销）

**效率补齐**
- 固定文件夹「一键打开全部」与「保存为书签文件夹」；设置页「从书签栏导入固定文件夹」（同名合并、URL 全局去重）
- 一键唤醒全部休眠标签；一键重置当前窗口全部标签缩放为 100%
- 搜索可选扩展到所有窗口（默认仅当前窗口，尊重产品原则）

**平台与配置**
- 快捷键体系补全：打开面板（`Ctrl+Shift+O`）、定位激活（`Ctrl+Shift+L`）、休眠非激活（`Ctrl+Shift+U`），设置页新增快捷键帮助面板（含跳转浏览器自定义）
- 设置页重构：首屏精简 +「高级设置」折叠区 + 分区计数徽章 + 设置搜索 + 「恢复默认」一键重置（新增 9 项可设置行为，共 33 项）
- 新增设置项：固定磁贴尺寸（紧凑/标准/大）、站点组配色（自动/统一中性）、新建标签位置（窗口末尾/激活标签后）、自动休眠系统通知开关、重复合并提示开关、工具栏角标模式（自动/标签数/重复数/关闭）、右键菜单总开关、地址栏命令总开关
- **算法改进（不增加设置项）**：域名聚合改为子域密度自动展开——同一注册域下不同子域数 ≥ 3 时，自动按子域独立成组（子域即业务），不再折叠到大组；少子域（≤2）保持折叠亚组。无需用户配置"分几级"。
- **成组阈值开放到 1**：聚合阈值选项新增「1（只要有标签就分组）」，单标签站点也独立成组（不落未分组）；自动原生组/快速整理的建组下限同步移除（跟随聚合阈值），阈值 1 时单标签站点也会自动建组。
- 移除：标签悬停预览功能（浏览器权限限制下截图不可靠、信息卡与行内内容重复，价值有限；随功能移除 `activeTab` 权限，权限面进一步收紧）
- 跨设备镜像同步：固定集合与设置分块镜像到浏览器同步通道（`chrome.storage.sync`），新设备首启自动恢复；超配额自动降级

### 隐私说明
- 新增权限：`contextMenus` / `omnibox` / `sessions` / `bookmarks` / `notifications`（用途见 PRIVACY.md，均不触网）

---

## [1.0.0] — 2026-08-22

首个公开发布版本（V1.0）。完整功能规格见 `docs/PRD.md`，架构见 `docs/ARCHITECTURE.md`。

### 新增能力

**标签视图与交互基座**
- 原生标签组、同站点自动聚合、未分组三区展示
- 标签行操作：切换 / 静音 / 固定 / 关闭（中键关闭）
- n× 重复计数、静音、拆分视图徽章
- 底部工具区：标签计数 + 一键清理重复

**固定空间**
- 固定文件夹：创建 / 重命名 / 删除 / 折叠 / 拖放排序 / 条目增删
- 挂起条目机制（组内新建 → 导航转正 → 全局 URL 去重）
- 会话绑定维护（站内跳转后标题/图标跟随更新）
- 永久固定图标：身份归一化、跨重启保留、单击打开 / 中键只关页
- 原生标签组可保存为固定文件夹

**搜索与键盘流**
- 模糊搜索：标题 / 网址 / 拼音首字母三目标索引，命中高亮
- 键盘导航：`↑↓` 选择、`Enter` 激活、`Esc` 关闭
- 快捷键：`Ctrl+Shift+F`（浏览器级）与 `⌘K` / `Ctrl+K`（面板内）

**多选与批量**
- 选择模式：`Shift` 连选、`Cmd/Ctrl` 点选、`Ctrl+A` 全选
- 批量操作条：关闭 / 固定 / 静音 / 移入文件夹
- 拖放：标签 → 固定图标区 / 固定文件夹；文件夹条目与文件夹排序

**多层撤销栈**
- 操作记录制（含关闭标签五元组 + 组名），栈深 10，FIFO 淘汰，持久化
- 恢复管线：按序重建 / 复用豁免 / 固定 / 静音 / 回组
- 状态提示条 + 撤销入口；所有关闭路径可撤销

**数据能力**
- 导出：全量 JSON（版本化）+ URL 列表双格式
- 导入：合并 / 覆盖两种模式，往返无损

**平台基座**
- 主题三态（跟随系统 / 亮 / 暗）落地
- 语言决策链（浏览器语言 → 设置覆盖），中 / 英双语
- 自制弹窗组件（替代 `window.prompt` / `confirm`）
- 弹窗快速切换器降级形态
- 隐私回归脚本（权限冻结 + 零网络调用扫描）

**打磨**
- 搜索模块懒加载（fuzzysort / pinyin-pro 独立 chunk，优化冷启动）
- 手动验收清单（`docs/CHECKLIST-V1.0.md`）
- 商店文案（`public/_locales`）、README、PRIVACY、本文件

### 质量门禁
- 行为规格测试 83 例全部通过
- `typecheck` / `lint` 零错误
- 标准版 + 兼容版双构建通过
- 隐私回归检查通过（权限 / 网络 / 数据三项零 diff）

---

> 后续规划见 `docs/PRD.md` 附录（V2.0 蓝图：归档中心、命名快照、浏览器同步通道、兼容变体正式交付）。
