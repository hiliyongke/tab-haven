# 更新日志

本文件遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 格式，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 2026-09-04

Tabs 首个对外公开版本。定位为**本地优先、无自有账号、无自有服务器**的标签工作台，只管理当前窗口的标签页。

### 新增

- **标签视图**：原生标签组、同站点自动聚合、未分组三区展示；`n×` 计数、静音、拆分视图徽章
- **组织**：固定文件夹（CRUD / 拖放排序 / 挂起转正 / 一键打开全部 / 存为书签）、永久固定图标、从书签栏导入固定文件夹
- **搜索**：标题 / 网址 / 拼音首字母模糊搜索，命中高亮，`↑↓` / `Enter` / `Esc` 键盘导航，可选扩展到所有窗口
- **拖拽整理**：dnd-kit 驱动的标签 / 固定磁贴 / 分组头 / 固定条目排序，拖入固定空间与文件夹时重复 URL 自动去重
- **快捷入口**：网页 / 链接 / 标签栏 / 工具栏图标右键菜单、地址栏 `th` 命令、工具栏角标
- **重复治理**：一键清理重复标签（保留激活 / 固定 / 最早打开者）
- **安全网**：多层撤销栈（栈深 10）、撤销历史面板（任意批次恢复 + 浏览器最近关闭）
- **会话快照**：命名保存 / 归档当前窗口 / 保存固定空间 / OneTab 文本导入 / 关窗自动快照
- **休眠与资源**：手动 / 批量 / 自动休眠（可撤销、支持域名白名单）、一键唤醒、重置标签缩放
- **数据**：完整备份导出导入（事务、失败整体回滚）、固定集合与设置经浏览器账号通道镜像同步
- **平台**：侧边栏主形态 + 弹窗降级形态、中英双语、主题三态、快捷键帮助面板

### 修复

- 设置页「高级与恢复」分区此前未标记 `collapsible`，实际未按设计默认折叠
- 「清除所有数据」确认弹窗的正文说明此前使用 10px 字号，违反设计令牌规范（正文须 ≥11px）
- **从设置页导出的备份此前永远不包含任何快照**：导出走内存快照桥，而设置页从不加载快照 store，
  用户用这份备份恢复会导致全部命名快照与关窗自动快照永久丢失。现改为直读仓库
- **备份导入可绕过禁缓存站点校验**：`noCachePatterns` 此前只在设置页编辑器内归一化，
  导入路径直接落盘，含 `https://*` 的备份文件可生成覆盖全部 HTTP(S) 流量的 DNR 规则。
  现归一化下沉到 `SettingsSchema`，成为唯一口径

### 安全

- 移除已随功能下线却仍留在 manifest 中的 `scripting` 权限（页面横幅已改为侧边栏角标呈现）
- 隐私回归脚本补齐 `host_permissions` / `optional_host_permissions` 检查，`<all_urls>` 纳入冻结清单
- 导入文件增加 5MB 体积上限，避免超大 JSON 在读取阶段卡死设置页
- **跨设备同步镜像新增开关且默认关闭**：此前固定集合与设置每次写入都无条件经
  `chrome.storage.sync` 上云，上传完整收藏 URL 与标题。现默认不同步，
  用户显式开启后才镜像；关闭时同步清空云端镜像
- 右键「加入文件夹」不再把非 http(s) 链接回落为原始字符串持久化（`javascript:` / `file:` 直接拒绝）
- 消息端点校验 `sender.id`，非本扩展来源的请求直接丢弃
- omnibox 建议文案转义 XML 标签；`pin:` 前缀增加 http(s) 白名单
- favicon 渲染增加地址白名单、data URL 长度上限与 `referrerPolicy="no-referrer"`
- 全部持久化 schema 补齐数组 / 字符串体积上限，防超大输入拖垮每次判定的 O(n) 遍历

### 修复

- **URL 比较键未归一化**：`https://A.COM/p` 与 `https://a.com/p`、显式默认端口与省略写法
  此前判为不同，重复标签检测在用户手输 URL 与导入备份两条路径上会静默漏判。
  现归一化主机名大小写并剥离默认端口；路径 / 查询 / 片段刻意保留
- 右键菜单提示此前硬编码英文，非英文用户界面看到英文；现走 i18n
- 「删除文件夹 / 重命名 / 新建」在写盘失败时仍提示成功；现按实际结果反馈
- `updateSettings` 落盘失败时 UI 停留在新值且无任何提示；现统一经 `tryUpdateSettings` 并明确报错
- `mutateSession` 串行队列在单次异常后会永久失效，后续所有会话写入静默丢失；现已自愈
- React 19 渲染期写 ref（并发渲染不安全）4 处，改由 `useEffect` 同步

### 测试

- 新增覆盖率门禁并分层设阈值（core 100% / platform ~70% / 全局 44%，ratchet 式锁定）
- 补齐 platform 层三座孤岛：`platform/tabs.ts`、`DataRepository`（坏数据隔离）、`RestoreEngine`
- 新增 URL 归一化等价类矩阵、撤销栈持久化往返、定位激活标签主链路、图标设计令牌守卫
- 性能测试加入预热与多轮中位数，阈值按数据规模分档，消除冷启动抖动
- 新增 `tests/setup.ts` 集中环境 polyfill，配置全局 `restoreMocks` / `clearMocks`
- 全仓统一 Prettier 格式，`format:check` 纳入 CI 首步

### 变更

- 项目改用 MIT 许可证（此前无 LICENSE 文件，与 README 声明的开源路径矛盾）
- README 移除三个指向已删除 `docs/` 文件的失效链接，改为可直接执行的分层约束清单
- 消息协议收敛为 `z.discriminatedUnion`，background 与侧边栏的手写分发链合并为单一入口
- 新增 CI 工作流：格式 → 类型 → 规范 → 文案 → 测试（含覆盖率）→ 构建 → 隐私回归
- **设置页拆分**：`SettingsPage.tsx`（1425 行）按职责拆为四个模块，
  形成单向依赖 `SettingsPage(编排) → settingSections(配置) → settingControls(展示控件)`，
  外加 `settingPresets`（预设画像与能力发现）。主组件不再承载 370 行配置体
- 通用合并写入器从 `dataStore` 下沉到 `platform/storage/coalescedWriter.ts`
- 侧边栏「定位激活标签」链路（含虚拟列表递归重试）抽出为 `useLocateActive`，
  使这条关键路径首次可被独立测试

## [1.0.0] 的已知限制

- 只发布标准版（侧边栏形态）；兼容变体（旧内核 Chromium 降级为弹窗）的构建管道已建成，随后续版本交付
- 备份为单一格式，不提供历史格式迁移；格式不符会被直接拒绝而非静默降级
- `sidepanel/App.tsx`（869 行）与 `dataStore.ts`（888 行）尚未完成拆分，
  前者已抽出 `useLocateActive`，后者已完成通用工具下沉；详细路径见 `REVIEW-2026-09-04.md` 第 5 节
