# 安全政策

Tabs 是一个**本地优先、无自有服务器**的浏览器扩展。我们的安全边界很窄，但正因为窄，越需要说清楚。

## 支持版本

我们只为**最新发布版本**提供安全修复。上架商店（Chrome Web Store / Edge Add-ons）会自动推送更新，请保持自动更新开启。

| 版本       | 支持状态          |
| ---------- | ----------------- |
| 最新正式版 | ✅ 提供修复       |
| 更早版本   | ❌ 不修复，请升级 |

## 报告漏洞

**请不要在公开 issue 中披露未修复的漏洞。**

请通过仓库的 [Security Advisories](../../security/advisories) 私下报告，并尽量包含：

- 漏洞类型与影响面（是否可读取/修改用户数据、是否可提权）
- 复现步骤（含扩展版本、浏览器及版本、操作系统）
- 可能的利用场景与危害评估

### 响应预期

| 阶段               | 时限                             |
| ------------------ | -------------------------------- |
| 确认收到           | 7 天内                           |
| 初步评估与影响判定 | 14 天内                          |
| 修复发布           | 视严重性而定，高危尽力在 30 天内 |

我们会在修复发布后，在 Advisory 中公开致谢报告人（除非你要求匿名）。

## 安全设计基线

报告漏洞前，以下行为是**刻意设计**，不属于漏洞：

1. **零出站网络请求** —— 扩展代码不含任何网络请求，也不发起任何出站连接。这是不可妥协的产品底线，由 `scripts/privacy-check.mjs` 在每次发布前强制校验。
2. **权限最小化且冻结** —— 权限清单在 `wxt.config.ts` 与 `PRIVACY.md` 中冻结，新增权限会令隐私回归检查失败。全站 host 权限走 optional、按需请求，默认不授予。
3. **不收集遥测** —— 无埋点、无崩溃上报。代价是我们无法感知线上故障，因此**诊断日志仅存于用户本地**，需要用户主动导出才能查看。
4. **数据不出设备（除非你主动开启同步）** —— 文件夹与设置的跨设备镜像由 `syncMirrorEnabled` 控制且**默认关闭**；开启后经浏览器厂商的账号同步通道传输，Tabs 不经手、也看不到这些数据。

## 不在范围内

- 需要用户已在本地获得管理员权限才能触发的攻击
- 浏览器自身（`chrome://` 内部页、扩展运行时）的漏洞 —— 请报告给对应浏览器厂商
- 仅影响已停止支持版本的缺陷
- 通过社会工程（诱导用户手动导入恶意备份文件）造成的影响 —— 我们已通过 schema 校验尽力收敛，但用户主动导入的内容无法完全免责
